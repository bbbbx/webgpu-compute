{
  "name": "webgpu-compute",
  "version": "1.0.0",
  "description": "A demo shows the WebGPU compute pipeline.",
  "scripts": {
    "dev": "live-server --host=localhost --port=5500 --watch=./src --open=./src/index.html --wait=500 | tsc -p ./tsconfig.json --watch",
    "build": "tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@webgpu/types": "^0.1.9",
    "live-server": "^1.2.1",
    "typescript": "^4.6.3"
  }
}
